#### csvtomarkup

```sh
npm install

# Put your input CSV here, with columns `Name, Age, Quote`.
vi input.csv # <-- put your input CSV here

# Output the generated markup to `output.html`.
npm run csvtomarkup

# Copy and paste that markup into the right place in `../index.html`.
```
